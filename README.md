# Documentation

# Comments from Members;

1. App Setup
    ```bash
    composer update
    php artisan app:setup
    npm run dev
    ```

2. Laravel Debug:
    ```bash
    tail -f laravel.log
    ```
